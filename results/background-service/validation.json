{
  "schema_version": 1,
  "phase": "mega-phase-b-background-service",
  "membrane_commit": "PENDING -- updated when the final PR (B4) of this mega-phase is squash-merged; individual pr fields below are updated as each PR B1-B4 actually merges",
  "generated_at": "2026-09-04T00:00:00Z",
  "pr_squash_shas": {
    "B1": "a88dfe4d7a8c519a6d1060c7291124aedb38690a"
  },
  "note": "Mega Phase B (PRs B1-B4): turns MEMBRANE into a persistent local AI backend managed as a systemd --user service, with real streaming, model-lifecycle hardening, and client integration. This file is seeded during PR B1 and extended by B2/B3/B4 -- it is NOT rewritten from scratch each PR, only appended to/corrected, matching results/runtime-service/validation.json's own precedent from Mega Phase A. REAL rows are genuine local invocations on this dev host (CPU-only build). SYNTHETIC rows are ctest-registered unit/integration tests with no real GGUF model dependency (CI-safe). SOURCE_ANALYSIS rows are direct code-reading confirmations, not runs.",
  "service_lifecycle": {
    "label": "REAL",
    "pr": "B1",
    "squash_sha": "a88dfe4d7a8c519a6d1060c7291124aedb38690a",
    "evidence": [
      "Real end-to-end local smoke on this dev host's own real systemd --user session (not a container, not a mock): `membrane model add`, `membrane service install` (real unit written to $HOME/.config/systemd/user/membrane.service, real `systemctl --user daemon-reload`), `membrane service start` (real `systemctl --user start`), `membrane service status` (real ActiveState/SubState/MainPID from `systemctl --user show` combined with a real GET /v1/status against the actually-running server), a real GET /health and GET /v1/status via curl, a real POST /v1/chat/completions against SmolLM2-135M-Instruct served entirely through the installed systemd unit (not `membrane serve` directly) -- generated real text, correct OpenAI-compatible JSON shape.",
      "Registry-not-hot-reloaded confirmed directly: a model registered AFTER the service was already running was correctly reported MODEL_NOT_FOUND until `membrane service restart` -- an honest, observed gap (registry hot-reload is explicitly a B3 task, not silently assumed to already work).",
      "`membrane service stop` confirmed via a subsequent curl connection failure (ECONNREFUSED) and `systemctl --user status` reporting the unit not found immediately after `membrane service uninstall`.",
      "`membrane service uninstall` confirmed to remove exactly the generated unit file and nothing else -- the pre-existing, unrelated `openclaw-gateway.service` unit already present in this same systemd --user directory was confirmed byte-identical (sha256) before and after the entire smoke sequence.",
      "No leftover `membrane serve` process after uninstall (confirmed via `pgrep`/journalctl's own real 'MEMBRANE server stopped' log line, not just an exit code).",
      "`membrane service install` correctly refuses to overwrite a same-named unit it did not generate (no MEMBRANE marker line) unless --force is passed -- confirmed both in this local smoke and in a CI-safe isolated-directory integration step (see ci_integration below).",
      "All test artifacts (registry entries, seeded server config, installed unit) were removed at the end of the smoke session -- the dev host's real $HOME/.config/membrane and $HOME/.local/share/membrane were confirmed empty/absent again afterward, matching their state before the smoke test began."
    ]
  },
  "systemd_unit_generation": {
    "label": "REAL",
    "pr": "B1",
    "evidence": [
      "Generated unit content directly inspected on-disk during the local smoke test: `[Unit]`/`[Service]`/`[Install]` sections present, `Type=simple`, `ExecStart=\"<real absolute exec path>\" serve` (quoted, no --port/--bind baked in), `Restart=on-failure`, `RestartSec=2`, `WantedBy=default.target`, and the MEMBRANE marker comment line.",
      "test_systemd_unit: 5 ctest-registered unit tests (pure, no filesystem access) covering unit-file shape, a custom Description override, an exec_path containing a space (systemd's own ExecStart= quoting, not a shell), the marker-detection predicate used by the overwrite-refusal check, and MEMBRANE_SYSTEMD_USER_DIR/XDG_CONFIG_HOME/HOME path-resolution precedence."
    ]
  },
  "server_config": {
    "label": "REAL",
    "pr": "B1",
    "evidence": [
      "Real seeded config file inspected on-disk during the local smoke test (written by `membrane service install` only when no config already existed).",
      "`membrane model use NAME` confirmed to persist default_model into the real config file and a subsequent `membrane serve`/service start picks it up (a chat request omitting \"model\" falls back to it) -- server.cpp's handle_chat_completions() request-validation change verified against the running service.",
      "test_server_config: 7 ctest-registered unit tests (pure, real temp files) covering defaults, nonexistent-file-is-defaults-not-error, atomic save+load round trip (no leftover .tmp.* file), malformed JSON / missing schema_version / unsupported schema_version all failing closed, and MEMBRANE_SERVER_CONFIG_PATH/XDG_CONFIG_HOME/HOME path-resolution precedence."
    ]
  },
  "fs_util": {
    "label": "SYNTHETIC",
    "pr": "B1",
    "evidence": [
      "test_fs_util: 5 ctest-registered unit tests covering mkdir -p semantics (including EEXIST-is-not-an-error on a repeat call), atomic write creating missing parent directories, no leftover .tmp.* file after a successful write, and a second write cleanly overwriting (not appending to) the first."
    ]
  },
  "subprocess_safety": {
    "label": "SYNTHETIC",
    "pr": "B1",
    "evidence": [
      "test_subprocess: 8 ctest-registered unit tests against real fork()/execvp() (ordinary coreutils -- echo/sh/sleep, never systemctl itself). Two tests specifically prove Section 11's 'no shell injection' requirement: a value containing shell metacharacters ($(), ;, |, &&, backticks) and a value containing an embedded newline are both delivered to the child as a single literal argv element, byte-for-byte, with zero shell interpretation -- confirmed by echoing them back and comparing exactly. Also covers stdout/stderr captured separately (never merged), a nonzero exit code propagating correctly, command-not-found reported as exit_code 127 rather than crashing the parent, an empty argv rejected before any fork(), and a real wall-clock timeout killing a long-running child (SIGKILL, waited-for, never left a zombie)."
    ]
  },
  "default_model_config": {
    "label": "REAL",
    "pr": "B1",
    "note": "Section 9 of the task: `membrane model use NAME` sets a persistent default so a chat request MAY omit \"model\" -- never forces one. Verified: a chat completion request against the real running service with no \"model\" field, after `membrane model use smolchat`, was correctly served by smolchat.",
    "disclosed_limitation": "default_model is read once, at `membrane serve`'s own startup (from server_config.h) -- changing it while the service is already running requires `membrane service restart` to take effect, exactly like the existing bind/port config fields. Documented in the CLI's own success message ('Takes effect on the next `membrane serve`/service restart'), not hidden."
  },
  "ci_integration": {
    "label": "SYNTHETIC",
    "pr": "B1",
    "evidence": [
      "New CI job service-lifecycle-tests: target-scoped build (no llama/ggml compilation, same trick as the pre-existing fallback-controller-tests job) of test_systemd_unit/test_server_config/test_fs_util/test_subprocess under ASan+UBSan.",
      "New packaging-smoke CI step ('CPU installed membrane service install/uninstall (isolated dirs)'): runs the real installed `membrane service install`/`uninstall` CLI dispatch against MEMBRANE_SYSTEMD_USER_DIR/MEMBRANE_SERVER_CONFIG_PATH pointed at isolated temp locations (never a real systemd --user session or this runner's own $HOME) -- verifies the generated unit's real on-disk content, that install refuses to overwrite a same-named unit lacking the MEMBRANE marker, and that uninstall removes it. `install` tolerates `systemctl --user daemon-reload` failing/being absent by design (its result is never checked); `service start/stop/status` themselves require a genuinely working systemd --user session and are intentionally NOT exercised in CI -- only in the real local smoke test above."
    ]
  },
  "server_streaming": {
    "label": "REAL",
    "pr": "B2",
    "squash_sha": "PENDING",
    "evidence": [
      "Real curl wire-format capture against a real running `membrane serve` instance (SmolLM2-135M-Instruct): Content-Type: text/event-stream, Cache-Control: no-cache, 21 real incremental `data: {...}` chunks (never the whole completion accumulated then sent as one chunk -- confirmed by inspecting each chunk's own `delta.content`, a few characters/one word at a time), a final `{\"choices\":[{\"delta\":{},\"finish_reason\":\"length\"}]}` chunk, then the literal `data: [DONE]`.",
      "Real `stream_options: {\"include_usage\": true}` request: one extra chunk (`choices: []`, populated `usage.{prompt_tokens,completion_tokens,total_tokens}`) appended right before `[DONE]`, matching real OpenAI's own opt-in convention -- omitted entirely (confirmed) when not requested.",
      "Real multilingual/emoji round trip: prompt asked the model to repeat '日本語 café 🎉' (3-byte CJK, 2-byte accented Latin, 4-byte emoji) verbatim; the real model echoed it, and the reconstructed streamed text (Python json.loads() on every single `data:` payload, which fails on invalid UTF-8) matched byte-for-byte -- no truncated/mangled character anywhere across the chunk boundaries.",
      "Real disconnect-cancellation proof: a streaming request for 200 tokens was aborted via `curl --max-time 0.5` mid-generation. The server process's own CPU TIME (ps -o cputime) was IDENTICAL immediately after the abort and 2 seconds later -- proof generation genuinely stopped (not merely that the TCP connection closed) rather than continuing to burn CPU on a completion nobody would ever read.",
      "Real sequential-request-after-cancellation proof: both a non-streaming and a second streaming request, issued immediately after the aborted one above against the SAME still-running server, completed successfully and quickly (0.19s for the non-streaming one) -- no leaked lock (st->mtx), no leaked thread, no corrupted session state.",
      "Real OpenAI Python SDK streaming session (openai 3.8.0, isolated venv): client.chat.completions.create(..., stream=True) iterated 21 real chunks via the SDK's own typed interface (never manual JSON parsing), correctly reconstructed content across them, and correctly reported finish_reason -- a non-streaming request against the same server in the same session confirmed API parity.",
      "Clean SIGTERM shutdown confirmed after all of the above (including the cancelled stream) -- no shutdown hang introduced by the new worker-thread/queue machinery."
    ]
  },
  "utf8_boundary_safety": {
    "label": "SYNTHETIC",
    "pr": "B2",
    "evidence": [
      "utf8_stream.{h,cpp}: a pure, llama/httplib-free module extracted specifically for direct unit testing (never only exercised indirectly through a real model's own token boundaries, which cannot be relied on to hit every case on demand).",
      "test_utf8_stream: 12 ctest-registered unit tests -- complete/incomplete 2/3/4-byte sequences (including the 4-byte emoji case), the empty string, malformed input (stray continuation bytes, an invalid lead byte), all bounded to never hold back more than 3 real incomplete bytes.",
      "A REAL bug was found and fixed by this test suite before it ever reached the real-model smoke test: the initial implementation's own lookback window was 3 bytes deep, which correctly detects INCOMPLETE tails but misidentified a COMPLETE 4-byte sequence's own trailing 3 continuation bytes as incomplete (its lead byte sits 4 positions back, outside a 3-byte window) -- caught immediately by test_complete_4byte_emoji failing, fixed by widening the scan window to 4 bytes while keeping the returned incomplete-length bound at 3."
    ]
  },
  "stream_queue_concurrency": {
    "label": "SYNTHETIC",
    "pr": "B2",
    "evidence": [
      "stream_queue.h: the bounded producer/consumer queue bridging the generation worker thread and the HTTP connection thread, extracted into its own header-only, llama/httplib-free module specifically so this project's own real concurrency (mutex + condition variable + a cross-thread atomic cancel flag) is unit-testable without a real model or a real HTTP round trip.",
      "test_stream_queue: 6 ctest-registered tests using REAL std::thread producers/consumers (not single-threaded call sequences dressed up as concurrency tests) -- FIFO ordering, a real bounded wait timeout, real backpressure (a push beyond MEMBRANE_STREAM_QUEUE_CAPACITY=64 genuinely blocks, proven by confirming it is STILL blocked after 150ms before a consumer drains one slot), a real 500-event producer/consumer pair with zero loss/duplication/reordering.",
      "A SECOND real bug was found and fixed by this test suite: the original push_blocking() used an unbounded std::condition_variable::wait() whose predicate included cancel_flag, but cancel_flag is a plain std::atomic<bool> set from OUTSIDE the queue (by server.cpp's stream_provide()/stream_release(), or by a test) -- nothing about setting it ever calls notify_all(), so a worker thread blocked on a full queue when a client disconnected would have hung FOREVER (a real deadlocked/leaked thread in production, not just a test artifact) -- caught directly by test_cancel_flag_wakes_blocked_push() hanging the whole test binary on first attempt. Fixed by polling with a bounded 50ms wait_for() instead of relying on every future cancel_flag setter to remember an explicit notify.",
      "Both bugs above were found BEFORE any sanitizer even ran, by the tests' own real multi-threaded execution -- ASan/UBSan (via the target-scoped service-lifecycle-tests CI job) and a dedicated target-scoped TSan job (stream-queue-thread-sanitizer, llama-free, same ASLR-disabled-retry precedent as server-thread-sanitizer) both then confirmed 0 findings on the fixed code.",
      "test_server (existing black-box HTTP suite, empty registry, no real model): 3 tests updated to reflect stream=true now being real (not rejected) -- every failure path reachable without a real model (unknown model, missing messages, malformed message) confirmed to still return a normal JSON error with Content-Type: application/json, never text/event-stream, proving headers are never committed to streaming until every pre-generation validation step has already succeeded."
    ]
  },
  "known_limitations": {
    "label": "SOURCE_ANALYSIS",
    "items": [
      "Model registry changes are not hot-reloaded by an already-running server/service -- confirmed directly (see service_lifecycle.evidence above). Deferred to PR B3 (Section 32 of the task).",
      "`membrane service start/stop/restart/status` genuinely require a real, working systemd --user session; there is no mock-systemctl layer -- `install`/`uninstall` alone are exercised in CI (see ci_integration), the full running-service lifecycle is validated locally only.",
      "No explicit model-lifecycle state machine or bounded pending-request queue yet -- the existing full-request-serialization mutex from Mega Phase A also serializes streaming requests behind non-streaming ones and vice versa (a second request, streaming or not, genuinely waits for a still-in-progress one) -- PR B3's job.",
      "Sampling is still greedy-only -- streaming reuses the exact same decode loop as non-streaming, unchanged by this phase.",
      "Cancellation is detected on the content-provider callback's own bounded poll (every 200ms while the queue is otherwise empty, or immediately on the next queue push/pop) -- not instantaneous, but bounded and proven to actually stop generation (see server_streaming.evidence above), not merely to close the connection."
    ]
  }
}
