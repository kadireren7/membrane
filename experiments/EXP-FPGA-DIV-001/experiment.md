# Experiment record: EXP-FPGA-DIV-001

Filled from [EXPERIMENT_TEMPLATE.md](../../EXPERIMENT_TEMPLATE.md).
Branch: `experiment/fp-divider-pipeline`.

## Experiment ID

`EXP-FPGA-DIV-001`

## Hypothesis

The existing `membrane_fp_divider` (a single, wide, un-pipelined
combinational IEEE-754 divider, see `docs/phase5-synthesizable-fpga.md`
section 7) can likely be replaced or supplemented, in at least some of
its 4 call sites, by an alternative that uses meaningfully fewer
synthesized cells and/or has a shorter combinational critical path,
without changing the datapath's bit-exact behavior. This is a
falsifiable claim to be tested in a **future Phase B**, not yet tested
here.

**Phase A scope (this record): characterize the baseline only.** No
alternative divider was designed, written, or synthesized in this
phase -- see `baseline.md`'s section 8 for candidate directions
proposed for Phase B, not implemented.

## Baseline tag/commit

Tag `v0.1.0-research`, commit `8298e953b792c78aa8604c7558ef701b2b862b28`
(the current stable public release). This experiment branch,
`experiment/fp-divider-pipeline`, was created from `main` at commit
`52b08959302174bb7da81850418ead0b44d9d6c5` (the current `main` HEAD at
experiment start, one docs-only commit ahead of the tag -- see
`docs/repository-boundary.md`/`docs/research-release-freeze.md` for why
`main` and the release tag are allowed to differ).

## Method

Phase A is pure characterization, no design work:

1. Located the divider RTL (`rtl/membrane_fp_divider.sv`) and its
   dependency (`rtl/valid_delay_line.sv`).
2. Found every instantiation site via `grep` across `rtl/` (excluding
   testbenches) and cross-checked against `rtl/membrane_quant_stream_top.sv`'s
   own hierarchy.
3. Derived baseline latency/initiation-interval numbers from the actual
   `DELAY`/`DIV_DELAY` parameter values used everywhere (`1`,
   hardcoded) and the consuming modules' own wiring (chained vs.
   parallel dividers), cross-checked against
   `docs/phase5-synthesizable-fpga.md` section 3's already-published
   figures.
4. Ran `yosys` (generic `synth` and `synth_ecp5`) on the standalone
   divider module, fresh, this session.
5. Re-ran `hierarchy -check -top membrane_quant_stream_top` (full RTL
   set) to confirm the whole design still elaborates cleanly.
6. Re-ran the existing 520,000-transaction Verilator cosimulation
   (`rtl/tb/tb_top_verilator.cpp`) against the unmodified RTL.
7. Re-ran the project's existing CI-equivalent local verification
   (Debug ctest, Release ctest, `verify-results.py`, `verify-paper.py`,
   `verify-outreach.py`) to confirm nothing else in the repository
   regressed.

No RTL file was modified. No new divider variant was written.

## Environment

This project's own development machine: 5.6 GiB RAM (the same
memory-constrained environment disclosed throughout this project's
prior phases). `tools/.local-yosys` (Yosys 0.33, git sha1 2584903a060)
and `tools/.local-verilator` (locally-extracted Verilator, no root
install) -- the same toolchain versions `docs/phase5-synthesizable-fpga.md`
used. No place-and-route tool, no Xilinx/Altera toolchain, no physical
FPGA board -- unchanged from every prior phase's disclosure.

## Model/dataset

Not applicable -- this is a pure RTL/synthesis-tooling experiment, no
LLM model or captured trace is involved. The Verilator cosimulation
uses the existing deterministic, fixed-seed golden vectors generated by
`rtl/tb/gen_top_x_vectors.c` and friends (120,000 blocks per format/
direction, same generators this project has always used for this test).

## Metrics

- Divider call-site count and role (constant vs. variable divisor).
- Per-unit latency (cycles) and initiation interval.
- Yosys generic and ECP5-mapped cell counts for the standalone divider.
- Verilator cosimulation transaction count and fail count.
- Local test-suite pass/fail counts (Debug, Release) and the three
  `verify-*.py` script pass counts.

All sourced in `baseline.md` and `results/baseline-synthesis.txt`, each
labeled MEASURED, SIMULATED, ESTIMATED, or UNAVAILABLE.

## Success criteria (Phase A)

- The divider RTL and every call site are correctly identified and
  cross-checked against at least two independent sources (`grep` +
  existing documentation).
- A real (not estimated) Yosys synthesis result exists for the
  standalone divider, in both a generic and a technology-mapped form.
- The existing 520,000-transaction Verilator cosimulation still passes
  at 0 fails against the unmodified RTL.
- Existing local verification (Debug/Release ctest, all three
  `verify-*.py` scripts) still passes unchanged.

All four were met -- see "Results" below.

## Failure criteria (Phase A)

- Any call site missed (a `membrane_fp_divider` instantiation not
  accounted for).
- Yosys synthesis failing to complete or reporting elaboration errors.
- The Verilator cosimulation reporting any of the 520,000 transactions
  as a mismatch (which would indicate this characterization work
  itself introduced a regression, since no RTL was supposed to change).
- Any existing test or verification script regressing.

None occurred.

## Resource budget

Expected: well under an hour of wall time, a few GB of peak RSS during
the Yosys `autoname` pass (the same bottleneck
`docs/phase5-synthesizable-fpga.md` already documented), no multi-hour
work. Actual: generic synth ~110s CPU / ~14s system / 1.94 GB peak;
`synth_ecp5` ~199s CPU / ~38s system / 1.49 GB peak; full-hierarchy
elaboration ~7s CPU; Verilator cosim 10.0s wall; full local
verification (Debug+Release ctest, 3 verify scripts) a few minutes.
Stayed within budget; the two Yosys runs were run sequentially, not
concurrently, given this machine's 5.6 GiB RAM ceiling.

## Checkpoints

None needed -- every step in this phase completes in well under a
minute except the two Yosys runs (~2-4 minutes CPU each), neither of
which needs resumable checkpointing.

## Results

See `baseline.md` (full characterization) and
`results/baseline-synthesis.txt` (raw synthesis summary). Headline
numbers:

- 4 divider instances in the full design (2 in `q4_scale`, chained;
  2 in `q8_scale`, parallel), all `DELAY=1`.
- Standalone divider: 1-cycle latency, initiation interval 1.
- `q4_scale`: 2-cycle latency (chained); `q8_scale`: 1-cycle latency
  (parallel); both II=1. Whole datapath: 7-cycle uniform latency
  (`L_MAX`), II=1 for every mode (source-derived, cross-checked against
  `docs/phase5-synthesizable-fpga.md`, not re-simulated cycle-by-cycle
  this session).
- Yosys generic synth: 10,234 cells. `synth_ecp5`: 73,629 cells
  (LUT4=37,998, CCU2C=10,173, PFUMX=15,848, L6MUX21=9,577,
  TRELLIS_FF=33) -- exactly reproduces the number already on record in
  `docs/phase5-synthesizable-fpga.md`.
- Verilator cosim: 520,000/520,000 transactions, 0 fails.
- Local verification: Debug ctest 28/28, Release ctest 28/28,
  `verify-results.py` 13/13, `verify-paper.py` 11/11,
  `verify-outreach.py` 17/17.

## Limitations

- No real Fmax/timing-closure number exists for any configuration
  (no P&R tool in this environment) -- the single biggest disclosed
  risk (section 7 of `baseline.md`) remains unquantified, not just
  unresolved.
- `q4_scale`/`q8_scale`/the full top-level design were not separately
  synthesized to completion this session (only the standalone divider
  was); their cell counts remain the pre-existing ~2x extrapolation
  from `docs/phase5-synthesizable-fpga.md`, not independently
  re-verified here.
- No real FPGA hardware, board, or vendor toolchain (Vivado/Quartus)
  was used anywhere in this experiment.
- This is a characterization-only phase; it makes no claim about
  whether any Phase B alternative will actually reduce cell count or
  improve timing closure -- that is exactly what Phase B would need to
  test.

## Decision

**Phase A accepted as complete.** The baseline is now characterized
with real, reproduced synthesis data and a source-grounded latency/II
table, and four concrete, disclosed-in-the-codebase-already candidate
directions for Phase B are recorded in `baseline.md` section 8. No
regression was introduced. Phase B (designing and evaluating an actual
alternative) is future work, not started here, and not committed to
any specific direction from the list in `baseline.md` section 8 yet.

## Phase B1 addendum (this experiment's own follow-on, same branch)

Everything above this section describes Phase A (characterization
only) and is unchanged, left as originally written per this project's
own disclosed-not-rewritten convention. Phase B1 implemented candidate
direction 1 from `baseline.md` section 8 (the power-of-two constant-
divisor shortcut for `q4_scale`'s `u_div_d`) -- full detail in
`phase-b1.md` and `results/b1-comparison.md`, summarized here:

- New files only: `rtl/experimental/fp_div/fp32_scale_neg_pow2.sv`,
  `rtl/experimental/fp_div/q4_scale_b1.sv`,
  `rtl/experimental/fp_div/membrane_quant_stream_top_b1.sv`,
  `rtl/tb/tb_fp32_scale_neg_pow2.cpp`,
  `rtl/experimental/fp_div/tb_top_verilator_variant.cpp`. No production
  RTL file (`rtl/q4_scale.sv`, `rtl/membrane_quant_stream_top.sv`,
  `rtl/membrane_fp_divider.sv`) was modified.
- Component differential test: 2,204,128 cases, 0 mismatches against
  the real `membrane_fp_divider` RTL for the exact `mx/-8.0f`
  operation.
- Full-datapath parity: 520,000/520,000 transactions, 0 fails, for
  both the baseline and B1 top-level variants (same shared C++
  testbench source, compiled per-variant).
- Synthesis (real, both generic and ECP5-mapped, both the standalone
  unit and the `q4_scale` integration point): standalone-unit ECP5
  cells -99.8%; `q4_scale`-level ECP5 cells only -2.2% (ABC's
  technology mapper was already sharing most of the two divider
  instances' cost in the baseline -- see `results/b1-comparison.md`
  section 4 for the full explanation).
- **Decision: CONTINUE** (exact and clean, but the real FPGA-resource-
  predicting metric shows only a small gain -- see `phase-b1.md`'s own
  "Decision" section for the full reasoning).

## Promotion status

`not proposed` -- this remains on `experiment/fp-divider-pipeline`,
pushed to the public repository per this project's open-development
policy (`docs/open-development-policy.md`), but **not merged into
`main`**. No pull request has been opened. Per
`docs/research-release-freeze.md`, nothing here is a verified public
claim of the `v0.1.0-research` release; it is disclosed,
research-in-progress work on a public branch. Phase B1's own decision
(CONTINUE, above) is an experiment-internal decision only, per this
project's contribution policy -- it does not itself authorize or imply
any merge into `main`.
